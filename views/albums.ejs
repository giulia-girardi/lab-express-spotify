<h1>Albums</h1>

<% listOfAlbumsCleaned.forEach(album => { %>
    <p> <%= album.name %> </p>
    <% if (album.images.length) {%> 
        <img src=" <%= album.images[0].url %>  " alt=""/>
    <% } %>
    <a href="/tracks/<%- album.id %>">View Tracks</a>
 <% }) %>